<div class="container container-left-align">
  <div class="main-page col-md-5 col-sm-12 col-xs-12">
    <%= link_to "Articles", articles_path, class: "btn btn-primary" %>
    <%= link_to "Videos", videos_path, class: "btn btn-primary" %>
    <ol>
      <% @articles.each do |post| %>
          <li>
            <%= post.postable.headline %></br>
          </li>
      <% end %>
    </ol>

    <%# @videos.each do |post| %>
    <%#= post.postable.name %></br>
    <%# end %>
  </div>
  <div class="col-md-3">
  </div>
  <div class="col-md-4">
  </div>
</div>
<%#= will_paginate @videos %>
<%#= will_paginate @articles %>